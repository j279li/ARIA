# ARIA Frontend

React/Vite frontend for the local ARIA backend.

Set `VITE_API_URL` in `.env.local` when the backend is not running at the
default `http://127.0.0.1:8000`.

```bash
npm install
npm run dev
```
